spring:
  config:
    activate:
      on-profile: test
  jpa:
    open-in-view: false
    properties:
      hibernate.default_schema: portfolio
  output:
    ansi:
      enabled: always

contact:
  brevo:
    api-key: k3y9f8s2-1a7b-4c6d-9e2f-4b1c8d7e5a0z
    to-email: test-to@example.com
    from-email: test-from@example.com
    from-name: Test Sender
  rate-limit:
    window-ms: 60000
    max-requests: 5

llm:
  ollama:
    base-url: http://ollama:11434
    moderation-model: ollama-model
    rag-generation-model: ollama-model
    rag-embedding-model: ollama-model
    moderation-max-tokens: 60
    rag-max-tokens: 512
    moderation-context: 2048
    rag-context: 8192
    keep-alive: 5m
    warm-up-interval-ms: 300000