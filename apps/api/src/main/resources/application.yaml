server:
  port: 9909
  forward-headers-strategy: framework
  servlet:
    context-path: /api

spring:
  jmx:
    enabled: false
  application:
    name: portfolio-api
  profiles:
    active: ${ACTIVE_PROFILE:}

  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 20000
      idle-timeout: 300000

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
        implicit-strategy: org.springframework.boot.hibernate.SpringImplicitNamingStrategy
    properties:
      hibernate:
        show_sql: false
        default_schema: portfolio
    show-sql: false
    open-in-view: true

  flyway:
    enabled: true
    clean-disabled: false
    locations: classpath:db/migration
    schemas: portfolio
    default-schema: portfolio
    baseline-on-migrate: true
    baseline-version: 0

  jackson:
    date-format: "yyyy-MM-dd'T'HH:mm:ss.SSSX"
    deserialization:
      fail-on-unknown-properties: false
    mapper:
      accept-case-insensitive-enums: true

cors:
  allowed-origins:
    - http://localhost:3000
    - https://portfolio.sfazzino.com

security:
  bootstrap:
    api-keys:
      admin: ${ADMIN_API_KEY:}
      client: ${CLIENT_API_KEY:}

contact:
  brevo:
    api-key: ${BREVO_API_KEY:}
    to-email: ${CONTACT_TO_EMAIL:}
    from-email: ${CONTACT_FROM_EMAIL:}
    from-name: ${CONTACT_FROM_NAME}
  rate-limit:
    window-ms: ${CONTACT_RATE_LIMIT_WINDOW_MS:60000}
    max-requests: ${CONTACT_RATE_LIMIT_MAX:10}

llm:
  ollama:
    base-url: ${OLLAMA_BASE_URL:http://ollama:11434}
    moderation-model: ${OLLAMA_MODEL}
    rag-generation-model: ${OLLAMA_RAG_GENERATION_MODEL}
    rag-embedding-model: ${OLLAMA_EMBEDDING_MODEL}
    moderation-max-tokens: ${OLLAMA_MODERATION_MAX_TOKENS:60}
    rag-max-tokens: ${OLLAMA__RAG_MAX_TOKENS:512}
    moderation-context: ${OLLAMA_MODERATION_CTX:2048}
    rag-context: ${OLLAMA_RAG_CTX:8192}
    keep-alive: ${OLLAMA_KEEP_ALIVE:5m}
    warm-up-interval-ms: ${OLLAMA_WARMUP_INTERVAL:300000}
  prompts:
    rag-path: classpath:prompts/knowledge_v1.txt
    moderation-path: classpath:prompts/contact_moderation_v1.txt

logging:
  level:
    org.hibernate.orm.connections.pooling: WARN
    sun:
      rmi:
        transport:
          tcp: ERROR
